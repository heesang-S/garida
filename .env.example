# Optional provider credentials for executor packages.
# Router core and MCP routing do not require provider credentials.

OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Optional local command override for Codex executor integrations.
GARIDA_CODEX_COMMAND=codex
